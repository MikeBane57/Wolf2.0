# Backlog

Ideas and future work for DonkeyCODE userscripts (not committed to a release yet).

## Worksheet / Pax connections

- **Click → connecting flights (Pax conx):** Add a click action (e.g. on a flight row or puck in the worksheet) that opens or surfaces **connecting flights that are tight or missing**, derived from **Pax connection** data shown in the worksheet context. Scope TBD: which DOM/cells define “close” vs “missing”, and whether to reuse Pax connections URLs (`Middle-click launcher` / pax-connections routes) or a new panel.

## Filters / UI

- **Auto-replace in filter:** Add an **auto replace** (or find-and-replace style) capability tied to the **filter** (where the app exposes a filter control—e.g. worksheet or schedule). Behavior TBD: replace-as-you-type, bulk replace on submit, or regex; confirm target surfaces in Ops Suite.
